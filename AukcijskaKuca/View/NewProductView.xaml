<Window x:Class="AukcijskaKuca.View.NewProductView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AukcijskaKuca.ViewModel"
        mc:Ignorable="d"
        Title="NewProductView" Height="300" Width="300">

    <Window.DataContext>
        <local:NewProductViewModel/>
    </Window.DataContext>

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <Label Content="Name of Article"/>
        <TextBox Text="{Binding NewProduct.Name}" TextWrapping="Wrap" 
                 HorizontalAlignment="Left" VerticalAlignment="Top"
                 Height="30" Width="170"
                 Margin="0 0 0 10"/>

        <Label Content="Price of article"/>
        <TextBox Text="{Binding NewProduct.Price}" TextWrapping="Wrap" 
                 HorizontalAlignment="Left" VerticalAlignment="Top"
                 Height="30" Width="170"
                 Margin="0 0 0 10"/>

        <Button Command="{Binding SaveCommand}" Content="Create new Product"
                HorizontalAlignment="Right" Width="120"/>
    </StackPanel>
</Window>
